window.generatePromptsDetailles = async function(storyboard) {
    if (!storyboard?.pages) {
        throw new Error("Storyboard invalide");
    }

    // ... code original avec sanitisation ...
    
    return promptsResult;
};
